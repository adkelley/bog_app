<%= form_for :creature, method: "post" do |f| %>
  <%= f.text_field :name %> <br>
  <%= f.text_area :description %> <br>
  <%= token_tag form_authenticity_token %>
  <%= f.submit "update creature" %>

<% end %>
